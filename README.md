# flink-linnear-regression
poc sobre flink implementando una linnear-regression bastante naïf (no usar en real!)

```sh
$ mvn clean package
```

y ejecutarlo desde flink con:

```sh
$ bin/flink run /YOURLOCALPATH/flink-linnear-regression/target/linnear-regression-0.0-SNAPSHOT.jar
```
